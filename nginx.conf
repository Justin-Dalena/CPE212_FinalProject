server {
    listen 8080;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index website.html;
    }

    # Optional: Disable access to .htaccess and hidden files
    location ~ /\. {
        deny all;
    }
}
